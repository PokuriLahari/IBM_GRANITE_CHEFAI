# ── Web framework ─────────────────────────────────────────────────────────────
flask==3.1.2
flask-cors==6.0.2
Werkzeug==3.1.3

# ── WSGI server (Linux / macOS only — skipped on Windows dev installs) ────────
# gunicorn is not available on Windows; comment this line out if pip complains.
# It is required inside the Docker container (Linux).
gunicorn==23.0.0; sys_platform != "win32"

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv==1.2.2

# ── IBM Watsonx.ai — raw REST (no SDK required) ───────────────────────────────
requests==2.33.1

# ── Vector store + embeddings ─────────────────────────────────────────────────
chromadb==1.1.1
sentence-transformers==5.6.0
torch==2.11.0
numpy==2.2.6
